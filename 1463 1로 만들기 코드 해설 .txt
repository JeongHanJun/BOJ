DP(Dynamic Programming) 이용
접근 방법은
 배열 만들고 각 원소에 arr[i] 의 값에 1로 만드는 카운트 횟수를 저장한다.
 그리고 점화식은
arr[i] = Math.min ( arr[i-1]+1 , arr[i/2]+1 , arr[i/3]+1 )이다
여기서 +1은 마지막에 연산하는 것.
이렇게 되려면 arr[0] , arr[1]은 초기값 을 넣어줘야한다.
입력되는 숫자가 1보다 크기떄문에 둘다 0으로 해도됨